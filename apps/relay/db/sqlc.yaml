version: "2"
sql:
  - schema: "schema.sql"
    queries: "queries.sql"
    engine: "sqlite"
    gen:
      go:
        package: "sqlc"
        out: "../internal/store/sqlc"
        emit_json_tags: true
        emit_prepared_queries: true
