version: "2"
sql:
  - schema: "db/schema.sql"
    queries: "db/queries.sql"
    engine: "sqlite"
    gen:
      go:
        package: "sqlc"
        out: "internal/store/sqlc"
        emit_json_tags: true
        emit_prepared_queries: true
